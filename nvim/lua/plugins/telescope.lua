local telescope_setup = require "telescope.builtin"

-- telescope_setup.live_grep {
--   vimgrep_arguments = {
--     "rg",
--     "--hidden", -- Add this line to include hidden files
--     "--no-ignore-vcs", -- Add this line to include files ignored by git
--   },
-- }
return {}
